apiVersion: v1
kind: Service
metadata:
  name: ui-portfolio-service
spec:
  selector:
    app: ui-portfolio
  ports:
  - port: 80              # Exposing port 80 on the service (optional: choose based on your needs)
    targetPort: 9090       # Ensure the container is listening on this port
    nodePort: 30081        # External port to expose the service
  type: NodePort
